package com.tado.android.report.interfaces;

import com.tado.android.report.ChartToolbarViewElement.ToolbarButtonTypeEnum;

public interface OnToolbarButtonListener {
    void onToolbarButtonClick(ToolbarButtonTypeEnum toolbarButtonTypeEnum, boolean z);
}
